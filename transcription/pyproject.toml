[project]
name = "unfoldiq-transcription"
version = "0.1.0"
description = "Isolated faster-whisper transcription environment for UnfoldIQ TTS Studio"
requires-python = ">=3.12"
dependencies = [
    "faster-whisper>=1.1.1",
    "av==16.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
